# 判断

1.任何需要通过总线(APB1,APB2,AHB)访问的外设(GPIO,USART,ADC),必须开启时钟(看系统框图:在数据手册上),否则寄存器读写无效

2.外设功能依赖时钟驱动,未使能时钟时,功能完全停滞

3.复用引脚重映射:需要用到AFIO/SYSCFG,必须开启AFIO/SYSCFG时钟

无需使能时钟

1.内核相关模块

Cortex-M内核内置组件(如NVIC,SysTick)无需手动使能时钟,系统启动后自动生效


**注:EXTI不需要时钟使能**
